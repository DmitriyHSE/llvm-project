import lit.formats

config.name = 'MarkUnused Plugin Tests'
config.test_format = lit.formats.ShTest()
config.suffixes = ['.c', '.cpp']

config.substitutions.append( ('%clang_cc1', 'clang++-18 -cc1') )
config.substitutions.append( ('%llvmshlibdir', '/src') )
